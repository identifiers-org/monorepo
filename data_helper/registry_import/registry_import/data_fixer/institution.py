import re

from halo import Halo

from classes.location import Location
from .utils import select_rows


# Creates a pool of proper defined institutions.
def create_institution_pool(bad_institition_pool):
    print(bad_institition_pool)
    return

# Maps an old institution to a new, shiny institution from the institution pool.
def map_institution(old_institution, institution_pool):
    result = []
    return result
